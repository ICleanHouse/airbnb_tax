# Frontend Features

Feature folders own workflow-level screens, local components, hooks, and helpers. Next.js route files under `frontend/app` should stay thin and delegate to these feature entrypoints.
